
// AI Service has been removed as per user request.
// This file is kept to avoid breaking import paths if any were missed, but exports nothing active.
export const explainSql = async (query: string, context: string): Promise<string> => {
  return "";
};
